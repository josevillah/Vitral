{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "permisos de la ventana principal, que hoy solo contiene el panel",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-close"]
}
